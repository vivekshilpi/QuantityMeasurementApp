# Monolith Archive

This folder preserves the original monolithic Spring Boot source that existed before the microservices migration.

Archived content:
- `src/main/java`
- `src/main/resources`
- `src/test`

The active application now lives in:
- `quantity-common`
- `auth-service`
- `quantity-service`
- `api-gateway`
- `service-registry`
